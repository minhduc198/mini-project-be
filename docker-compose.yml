services:
  api:
    container_name: mini-project-be-api
    build:
      context: .
      dockerfile: Dockerfile
    image: mini-project-be:compose
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - PORT=5000
    ports:
      - "5001:5000"
    restart: unless-stopped
